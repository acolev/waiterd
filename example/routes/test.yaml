endpoints:
  - path: /api/test
    cache_ttl: 60s
    method: GET
    backend:
      service: test
      path: /info
      method: GET
  - path: /posts/{id}
    cache_ttl: 120s
    middlewares: ["auth"]
    method: GET
    calls:
      - name: test
        service: test
        path: /info
        method: GET
      - name: post
        service: jsonp
        path: /posts/{id}
        method: GET
        mapping:
          title: "title"
          body: "body"
    response_mapping:
      title: "post.title"
      description: "post.body"
      message: "test"

